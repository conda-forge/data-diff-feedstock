{% set name = "data-diff" %}
{% set version = "0.6.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 6de9c5a8e7b6d5467418ed79b287e1c2c0a525891413d17a8d1b6777aa9e85f8

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  entry_points:
    - data-diff = data_diff.__main__:main

requirements:
  host:
    - pip
    - poetry-core >=1.0.0
    - python >=3.7,<4.0
  run:
    - click >=8.1,<9.0
    - dsnparse
    - packaging
    - python >=3.7,<4.0
    - rich
    - runtype >=0.2.6,<0.3.0
    - sqeleton ==0.0.8
    - toml >=0.10.2,<0.11.0

test:
  imports:
    - data_diff
  commands:
    - pip check
    - data-diff --help
  requires:
    - pip

about:
  home: https://github.com/datafold/data-diff
  doc_url: https://data-diff.readthedocs.io/en/latest/
  summary: Command-line tool and Python library to efficiently diff rows across two different databases.
  license: MIT
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - thewchan
